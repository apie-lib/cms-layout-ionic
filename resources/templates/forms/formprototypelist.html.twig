<template id="tmpl{{ property('name').getPrototypeName() }}">{{ component('__proto__')|e }}</template>
<apie-form-list class="unhandled-list" replace-string="{{ property('name').getPrototypeName() }}" name="{{ property('name') }}" template-id="tmpl{{ property('name').getPrototypeName() }}"></apie-form-list>
<input type="hidden" name="{{ property('name').getTypehintName() }}" value="array"/>
<apie-script>
(function (elm) {
  elm.classList.remove('unhandled-list');
  elm.value = {{ property('value') | json_encode | replace({'<': '&lt;'}) | raw }};
  elm.validationErrors = {{ property('validationErrors') | json_encode | replace({'<': '&lt;'}) | raw }};
}(document.querySelector('apie-form-list.unhandled-list')));
</apie-script>