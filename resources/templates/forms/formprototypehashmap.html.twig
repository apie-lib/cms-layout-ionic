<template id="tmpl{{ property('name').getPrototypeName() }}">{{ component('__proto__')|e }}</template>
<apie-form-hashmap class="unhandled-hashmap" replace-string="{{ property('name').getPrototypeName() }}" name="{{ property('name') }}" template-id="tmpl{{ property('name').getPrototypeName() }}"></apie-form-list>
<input type="hidden" name="{{ property('name').getTypehintName() }}" value="object"/>
<apie-script>
(function (elm) {
  elm.classList.remove('unhandled-hashmap');
  elm.value = {{ property('value') | json_encode | replace({'<': '&lt;'}) | raw }};
  elm.validationErrors = {{ property('validationErrors') | json_encode | replace({'<': '&lt;'}) | raw }};
}(document.querySelector('apie-form-hashmap.unhandled-hashmap')));
</apie-script>
