<div style="width: 100%; padding: 15PX; float: right; display: table;">
  {{ component('filterColumns') }}
  <div style="display: table-cell; vertical-align: middle" class="resource-actions resource-actions-unprocessed">
    {% for action in property('actions') %}
      <ion-button variant="{{ action.getVariant().value }}" data-small-page="{{ action.isSmallPage() | json_encode }}" data-url="{{ action.getUrl(property('config')) }}">{{ action.name }}</ion-button>
    {% endfor %}
  </div>
  <script>{{ assetContent('apply-resource-action.js') }}</script>
</div>